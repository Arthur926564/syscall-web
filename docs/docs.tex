\documentclass{article}
\usepackage{hyperref}
\usepackage{booktabs}
\usepackage{tikz}
\usepackage{graphicx}
\usepackage{microtype}
\usepackage{geometry}
\geometry{margin=1in}

\title{Design and Performance Evaluation of a Minimal Non-Blocking HTTP/1.1 Server}
\author{Arthur Herbette}
\date{April 11, 2026}

\begin{document}
\maketitle

\begin{abstract}
This report presents the design and performance evaluation of a minimal HTTP/1.1 server
implemented in C. The server relies on non-blocking I/O, an epoll-based event loop, and a
multithreaded worker architecture using \texttt{SO\_REUSEPORT} to handle concurrent connections
efficiently. We analyse architectural choices, implementation challenges, and evaluate performance
under synthetic workloads using \texttt{wrk}. The final implementation achieves throughput within
run-to-run variance of \texttt{nginx} on static file serving.
\end{abstract}

\section{Introduction}

The goal of this project was to gain a deeper understanding of HTTP and network programming.
You can find the GitHub repository at this link. I started this project as a simple blocking,
single-threaded, loop-based server. After understanding those fundamentals I implemented header
parsing which enabled persistent (keep-alive) connections. The goal after that introduction was
to have a very efficient and scalable server. Those reasons motivated the transition to a
non-blocking server architecture, allowing the server to handle multiple connections concurrently
using asynchronous I/O, and eventually to a fully multithreaded design.

\section{Architecture}

The server follows an event-driven architecture. It started as a single-threaded reactor pattern
and evolved into a multi-threaded design where each worker thread owns its own epoll instance
and its own listen socket via \texttt{SO\_REUSEPORT}. The architecture is divided into 7 layers:

\begin{itemize}
    \item \textbf{core}: coordinates the server and orchestrates interactions between components
    \item \textbf{epoll}: contains structs to use epoll in the server loop
    \item \textbf{HTTP}: parses requests, turns plain text into \texttt{http\_request\_t}
    \item \textbf{net}: handles connection setup, TCP listen and accept
    \item \textbf{os}: provides low-level read/write operations and filesystem access
    \item \textbf{static}: handles all transactions which use static files (i.e., \texttt{index.html})
    \item \textbf{util}: contains all utilities of the server; at the moment it contains only
          \texttt{buffer\_t} which serves to store and consume all data from the connection
\end{itemize}

The system is divided into layers so that each component exposes a clear abstraction and
minimises coupling with the others.

\medskip
To better understand this server we can first explain what each layer does in the stack:

\begin{center}
\begin{tikzpicture}
  \foreach \label/\y in {
    {OS Layer}/0,
    {I/O Multiplexing (epoll)}/1,
    {Connection Layer}/2,
    {HTTP Layer}/3,
    {Application Layer (static file server)}/4
  } {
    \draw (0, \y*0.7) rectangle (8, \y*0.7+0.6);
    \node at (4, \y*0.7+0.3) {\label};
  }
\end{tikzpicture}
\end{center}

\noindent Incoming data flows upward through the stack (OS $\to$ HTTP), while responses are
generated at the application layer and propagated downward to the socket.

\paragraph{OS Layer} The OS layer's responsibilities are: sockets, file descriptors,
read/write/accept, and filesystem access.

\paragraph{Event Layer} The event layer (epoll) turns blocking OS events into asynchronous
events. The question it tries to answer is: \emph{which fd is ready?}

\paragraph{Connection Layer} The connection layer abstracts over raw sockets; its
responsibilities are: input/output buffers, write offsets, connection state, and the
keep-alive lifecycle.

\paragraph{HTTP Layer} It only works with plain text. The goal is to: parse requests,
detect headers, handle keep-alive, and build responses. It transforms raw bytes into a
structured request.

\paragraph{Application Layer (static)} This is the highest-level logic. At the current
moment, as it serves only GET, it only serves files, but could later become: API endpoints,
dynamic content, routing.

\subsection{Multithreaded Worker Model}

The final architecture uses $N$ worker threads (currently 8), each running its own epoll
loop. Rather than having a single main thread accept connections and distribute them to
workers via a queue, which introduces cross-thread synchronisation on every connection;
each worker binds its own copy of the listen socket using \texttt{SO\_REUSEPORT}. The kernel
itself distributes incoming connections across workers with no mutex, no \texttt{eventfd}
wakeup, and no shared queue. Each worker is entirely self-contained:

\begin{itemize}
    \item its own \texttt{epoll} instance
    \item its own listen socket (\texttt{SO\_REUSEPORT})
    \item its own connection pool (\texttt{conn\_pool\_t})
\end{itemize}

This design eliminates the cross-thread dispatch overhead that was previously responsible for
roughly 20\,\% of latency under load. Compared to the earlier dispatch model, the hot path
per connection drops from: \texttt{lock $\to$ memcpy $\to$ eventfd write $\to$ wakeup $\to$
lock $\to$ drain $\to$ epoll\_ctl} to simply: \texttt{epoll fires $\to$ accept4 $\to$
epoll\_ctl}.

% \subsection{State Machine}
%
% One of the issues during the development of this server was that, as a connection does not
% complete in one step, when receiving an event for a connection we needed to know the state of
% the connection to be able to proceed with the handling. To resolve this issue, I added the
% following state machine:
%
% \begin{center}
% \begin{tikzpicture}[->, >=stealth, node distance=2.5cm, every node/.style={draw, rounded corners, minimum width=2cm, minimum height=0.7cm, align=center}]
%   \node (reading) {reading};
%   \node (writing) [right=16cm of reading] {writing};
%   \node (closed)  [right=16cm of writing] {closed};
%   \draw (reading) -- node[above, draw=none] {request complete} (writing);
%   \draw (reading.south) .. controls +(0,-1) and +(0,-1) .. node[below, draw=none] {malformed} (closed.south);
%   \draw (writing) -- node[above, draw=none] {finished} (closed);
%   \draw (writing.north) .. controls +(0,1) and +(0,1) .. node[above, draw=none] {keep-alive} (reading.north);
%   \draw (reading) .. controls +(-1.5,0) and +(-1.5,0) .. node[left, draw=none] {incomplete} (reading);
% \end{tikzpicture}
% \end{center}
%
\section{Implementation Challenges}

The primary challenges were related to incremental I/O and state management. Unlike blocking
implementations, requests may arrive in partial segments, requiring persistent buffering and
repeated parsing attempts. Handling keep-alive connections introduced additional complexity,
since multiple requests can coexist within the same input buffer.

A subtler challenge was the interaction between edge-triggered epoll (\texttt{EPOLLET}) and
partial reads. With level-triggered epoll the kernel re-notifies as long as data is available,
which is forgiving of incomplete reads. With \texttt{EPOLLET} the notification fires exactly
once on the state transition; if the entire available data is not drained in that single
dispatch, the connection silently stalls. This required careful loop structures in
\texttt{handle\_read} to always drain until \texttt{EAGAIN}.

Another challenge specific to the multithreaded design was ensuring correct use of
\texttt{epoll\_ctl}. The \texttt{EPOLL\_CTL\_MOD} operation requires the file descriptor to
already be registered via \texttt{EPOLL\_CTL\_ADD}; calling \texttt{MOD} on an unregistered
fd returns \texttt{ENOENT} silently, leaving the connection permanently stalled. Systematic
error checking on all \texttt{epoll\_ctl} calls was necessary to surface these issues during
development.

\section{Experimental Setup}

Benchmarks were performed using \texttt{wrk}:
\begin{verbatim}
wrk -t8 -c400 -d30s http://127.0.0.1:8080/<file>
\end{verbatim}

The setup is a ThinkPad T16 with an Intel Core Ultra 7 155U (14 cores) @ 4.80\,GHz with
32\,GiB of RAM running Arch Linux. All benchmarks compare directly against \texttt{nginx}
running on the same machine on port 8081, serving identical files.

\section{Discussion}

\subsection{Effect of Keep-Alive}

The transition to a non-blocking architecture significantly increased throughput, especially
when combined with persistent connections. Without keep-alive, one request corresponds to
exactly one TCP connection. This means that for every request the server must:
\texttt{accept()}, TCP handshake, read the request, write the response, close socket. With
keep-alive, one connection can carry many requests. The connection stays open, so we avoid
repeating the accept and close process, which reduces syscall count and kernel allocations.

\medskip
\noindent Measured impact of keep-alive (single-threaded baseline, \texttt{ab -n 10000 -c 100}):

\begin{center}
\begin{tabular}{lrr}
\toprule
\textbf{Mode} & \textbf{Req/s} & \textbf{Notes} \\
\midrule
Without keep-alive & 51,105 & full TCP handshake per request \\
With keep-alive    & 152,935 & connections reused across requests \\
\bottomrule
\end{tabular}
\end{center}

Adding keep-alive does not especially reduce the latency of each individual request but adds
significant throughput and scalability by removing the TCP setup cost.

\subsection{Optimisation Journey}

The server was profiled iteratively using \texttt{perf record} and \texttt{perf report} after
each change. The following optimisations were applied in order of impact:

\begin{enumerate}
    \item \textbf{SO\_REUSEPORT per-worker sockets}: eliminated the centralised
    accept-and-dispatch model with \texttt{eventfd} wakeups. Each worker binds its own
    listen socket; the kernel distributes connections with no cross-thread synchronisation.

    \item \textbf{Keep-alive typo fix}: a typo in the HTTP version comparison
    (\texttt{HTTTP/1.1} instead of \texttt{HTTP/1.1}) caused every connection to be treated
    as HTTP/1.0 and closed after each response. Fixing this alone approximately doubled
    request throughput by enabling connection reuse.

    \item \textbf{Parser cleanup}: replaced \texttt{strncpy}-based header copying with
    bounded \texttt{memcpy} using lengths already computed during scanning. Eliminated
    in-place buffer mutation (writing \texttt{\textbackslash{}0} into the buffer and
    restoring) which was cache-unfriendly and fragile.

    \item \textbf{Eliminated \texttt{dup()} per request}: the original code called
    \texttt{dup()} on the cached file descriptor so that \texttt{sendfile} could have its
    own file offset. Since \texttt{sendfile} accepts an explicit \texttt{offset} pointer,
    the \texttt{dup()} is unnecessary; removing it eliminated a syscall and a
    \texttt{close()} on every request.

    \item \textbf{TCP\_CORK gating}: \texttt{TCP\_CORK} was being set and unset on every
    request regardless of file size, adding two \texttt{setsockopt} syscalls per response.
    Gating behind a file-size threshold (only cork when file $> 16\,\mathrm{KiB}$) removed
    this overhead for the common case of small files.
\end{enumerate}

\subsection{Limitations}

Performance behaviour can be interpreted through three regimes: connection-bound, CPU-bound,
and network-bound execution. Enabling persistent connections moved the server from a
connection-bound regime to a CPU-bound regime, significantly increasing throughput. The
multithreaded design then distributed that CPU work across cores, pushing throughput further.

The remaining bottlenecks identified by \texttt{perf} are largely irreducible within the
\texttt{epoll} architecture: HTTP parsing ($\sim$11\,\%), \texttt{epoll\_ctl} ($\sim$10\,\%),
\texttt{read}/\texttt{write} syscalls ($\sim$10\,\% each), and \texttt{sendfile}
($\sim$7\,\%). These represent the fundamental cost of serving a file over a socket.

\section{Benchmark Results}

\paragraph{Final benchmark --- $100\,\mathrm{KiB}$ file}
After all optimisations, the server sustained $248{,}280$ requests per second with a mean
latency of $1.33\,\mathrm{ms}$ and a transfer rate of $23.70\,\mathrm{GB/s}$. Under the
same conditions \texttt{nginx} achieved $251{,}822$ requests per second, placing the server
at approximately $98.6\,\%$ of \texttt{nginx} throughput --- a gap within run-to-run
variance.

\begin{table}[h]
\centering
\begin{tabular}{lrrr}
\toprule
\textbf{Server} & \textbf{Req/s} & \textbf{Avg latency} & \textbf{Transfer rate} \\
\midrule
This server & $248{,}280$ & $1.33\,\mathrm{ms}$ & $23.70\,\mathrm{GB/s}$ \\
\texttt{nginx} & $251{,}822$ & $4.01\,\mathrm{ms}$ & $24.07\,\mathrm{GB/s}$ \\
\bottomrule
\end{tabular}
\caption{Static file throughput, $100\,\mathrm{KiB}$ file, 8 threads, 400 concurrent
connections, 30\,s run.}
\end{table}

\paragraph{Profiling summary}
Successive \texttt{perf} reports showed the hotspot distribution converging toward
irreducible costs. The final profile attributes approximately $11\,\%$ of cycles to HTTP
request parsing, $10\,\%$ each to \texttt{epoll\_ctl} and \texttt{read}/\texttt{write}
syscalls, and $7\,\%$ to \texttt{sendfile}. No single user-space function dominates; the
remaining overhead is split across \texttt{buffer\_ensure\_writable},
\texttt{http\_response\_write\_file}, and kernel time. This distribution indicates that the
implementation has reached the practical ceiling of the \texttt{epoll}-based architecture
for this workload.

\paragraph{Connection stability}
During long keep-alive stress tests, sporadic \texttt{ECONNRESET} errors were observed,
consistent with \texttt{wrk} tearing down connections mid-flight at the end of a test window.
These are suppressed in the final implementation by ignoring \texttt{ECONNRESET} and
\texttt{EPIPE} on the read and write paths respectively. No errors were observed under normal
single-connection \texttt{curl} workloads once the keep-alive parsing bug was corrected.

\section{Future Work}

\paragraph{io\_uring}
The next architectural step would be to replace the \texttt{epoll} event loop with
\texttt{io\_uring}. Rather than one syscall per I/O operation, \texttt{io\_uring} allows
submitting a ring of operations and collecting completions in bulk via a single
\texttt{io\_uring\_enter} call. This would reduce the cost of \texttt{epoll\_ctl},
\texttt{read}, \texttt{write}, and \texttt{sendfile} --- currently responsible for roughly
$35\,\%$ of cycles --- at the cost of a significant rewrite of the event loop and connection
state machine.

\paragraph{Precomputed response headers}
Currently \texttt{http\_response\_write\_file} builds the response headers at request time
via several \texttt{buffer\_append} calls. Since the files are cached at startup with known
sizes and content types, the full response header string could be precomputed once per
cached file and stored in \texttt{static\_file\_entry\_t}, reducing the per-request header
path to a single \texttt{memcpy}.

\paragraph{HTTPS}
Moving from HTTP to HTTPS requires an SSL/TLS certificate. OpenSSL is the standard way of
doing it. Adding encryption changes the architecture by introducing a new TLS layer between
the HTTP and connection layers. TLS will introduce CPU cost due to encryption and handshake
overhead, which makes performance tracking more difficult. This feature is planned after the
above optimisations.

\section{Conclusion}

As we have seen, non-blocking I/O combined with keep-alive and a multithreaded
\texttt{SO\_REUSEPORT} design allowed the server to reach throughput within run-to-run
variance of \texttt{nginx} on static file serving.

This project really taught me how networking works at a very low level. I also really
enjoyed going from a simple kind of `single-cycle' server, into `multi-cycle', into a
`superscalar' processor. And as a natural extension, the architecture would become a
`multi-core' server, which is exactly what it did.

\end{document}

